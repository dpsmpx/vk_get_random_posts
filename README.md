# VK Get Random Posts

Простой одностраничный HTML-скрипт для отображения 5 случайных постов из ваших подписок ВКонтакте. Проект создан для того, чтобы быстро просматривать свежий контент из разных источников, не заходя в саму социальную сеть.

Скрипт запрашивает посты, опубликованные **за последний месяц**, и кэширует результат в браузере для мгновенной загрузки при повторном открытии страницы.

## 🚀 Назначение

*   **Быстрая лента новостей:** Получите подборку из 5 случайных постов, чтобы быть в курсе событий в ваших группах.
*   **Снижение API-нагрузки:** Благодаря кэшированию, скрипт не делает лишних запросов к API ВКонтакте при каждом открытии.
*   **Простота и автономность:** Работает в любом современном браузере без необходимости установки серверного ПО. Все, что нужно — два HTML-файла.
*   **Полный контроль:** Вы сами управляете списком групп и имеете полный доступ к коду.

## 🛠️ Как пользоваться

Для работы скрипта не требуется веб-сервер. Достаточно открыть файл `vk_posts.html` в браузере.

**Шаг 1: Настройка списка групп (`subscriptions.html`)**

Скрипт читает список ваших подписок из файла `subscriptions.html`. У вас есть два способа его заполнить:

1.  **Вручную (рекомендуется):**
    *   Откройте файл `subscriptions.html` в текстовом редакторе.
    *   Скопируйте адрес и название интересующей вас группы ВКонтакте.
    *   Вставьте их в файл по следующему шаблону:
        ```html
        <div class="item">
          <div class='item__main'><a href="АДРЕС_ГРУППЫ">НАЗВАНИЕ_ГРУППЫ</a></div>
        </div>
        ```
    *   В репозитории уже есть пример с популярными сообществами.

2.  **Через архив данных VK:**
    *   Запросите свой архив данных ВКонтакте (Настройки -> "Защита и вход" -> "Скачать свои данные").
    *   После получения архива найдите в нем файл `subscriptions.html`.
    *   Скопируйте этот файл в папку с проектом.

**Шаг 2: Получение ключа доступа (Access Token) VK API**

Для работы с API ВКонтакте скрипту необходим ключ доступа.

1.  Убедитесь, что VPN **отключен**.
2.  Перейдите по [этой ссылке для получения токена](https://oauth.vk.com/authorize?client_id=2685278&scope=wall,offline&redirect_uri=https://oauth.vk.com/blank.html&display=page&response_type=token&v=5.199). Мы используем ID приложения Kate Mobile, так как оно не привязывает токен к IP-адресу.
3.  Нажмите **"Разрешить"** на странице запроса прав.
4.  Вы будете перенаправлены на пустую страницу с ошибкой — **это нормально**.
5.  Скопируйте из адресной строки вашего браузера длинную последовательность символов, которая находится между `access_token=` и `&expires_in`.

**Шаг 3: Запуск**

1.  Откройте файл `vk_posts.html` в текстовом редакторе.
2.  Найдите строку:
    ```javascript
    const accessToken = 'ВАШ_КЛЮЧ_ДОСТУПА_ОТ_KATE_MOBILE';
    ```
3.  Вставьте скопированный ключ доступа вместо `ВАШ_КЛЮЧ_ДОСТУПА_ОТ_KATE_MOBILE`.
4.  Сохраните файл `vk_posts.html`.
5.  Откройте этот же файл в вашем браузере. При первом запуске нажмите кнопку "Обновить ленту". При последующих открытиях страницы вы увидите посты из кэша.
6.  Чтобы обновить посты, просто нажмите "Обновить ленту" ещё раз.

## 📂 Файлы в репозитории

*   `vk_posts.html` — основной файл. Содержит всю логику скрипта, стили и HTML-структуру для отображения постов.
*   `subscriptions.html` — файл со списком ваших подписок. Скрипт парсит его для получения адресов групп.

## 💡 Возможные улучшения

*   Добавить фильтрацию постов по ключевым словам.
*   Реализовать "черный список" для групп, которые нужно временно исключить из выборки.
*   Добавить отображение изображений и других вложений из постов.
